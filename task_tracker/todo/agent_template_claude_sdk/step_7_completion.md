# Step 7: Завершение плана

> Status: pending

## Чеклист

- [ ] Все шаги плана выполнены ([x] в PLAN.md)
- [ ] Критерии готовности из PLAN.md проверены (каждый — командой или тестом)
- [ ] Smoke test end-to-end:
  - [ ] `claude login` сделан на тестовой машине
  - [ ] `python agents/claude/main.py` запускается без ошибок
  - [ ] За один cron-тик: прочитал Bus → классифицировал → сгенерил дайджест → записал 0–2 строки в Bus
  - [ ] При повторном запуске не дублирует ничего
  - [ ] Уложился в 5-минутный hard-timeout
- [ ] Не сломано: `curator/` и `test_stand.py` продолжают работать
- [ ] `members.json` валиден (никаких случайных правок)
- [ ] `bus-protocol.md` обновлён (добавлены `agent_pick`, `agent_summary`)
- [ ] `agents/claude/README.md` описывает агента с тремя сценариями деплоя + ToS-секцией
- [ ] `.gitignore` покрывает `agents/*/.env`, `agents/*/.local/`
- [ ] Мусор убран (нет временных файлов, тестовых БД в git, дебажных print'ов)
- [ ] Файлы не превышают разумные размеры (≤300 строк там где это правило применимо)
- [ ] Пользователь (Ренат) подтвердил формулировку ToS-disclaimer'а
- [ ] В backlog появилась заметка `agents_read_community_db.md` про подключение общей БД сообщений к агентам
- [ ] Статус в PLAN.md → done
- [ ] Папка перемещена: `task_tracker/todo/agent_template_claude_sdk/` → `task_tracker/done/agent_template_claude_sdk/`

## Если smoke-test провалился

Не помечать план как завершённый. Создать заметку в backlog:
- Что именно не работает
- На каком шаге всплыло
- Что пробовали

И вернуть план обратно в `todo/` с обновлённым PLAN.md.

## После завершения

Когда план в `done/` — короткое уведомление пользователю:
- Что сделано
- Какие компромиссы пришлось принять (если были)
- Что появилось в backlog
- Что предлагается следующим шагом — по плану это **Опенкло (Sonnet API)** и **Гермес (Haiku API)** как отдельные планы. Структура `agents/claude/` станет образцом для них.
