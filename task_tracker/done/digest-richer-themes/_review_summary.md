# Review Summary — digest-richer-themes

> Дата: 2026-06-18
> Ревью: code + risks + structure + coverage

## Критичное (блокирует)

1. **`_parse_json_array` НЕ подходит для объекта `{name, intrigue}`** [code #2,
   подтверждено в коде]. Функция в `synthesis.py:290` заточена под МАССИВ: fallback
   ищет `s.find("[")` / `s.rfind("]")` (строки 307-309). Для JSON-ОБЪЕКТА
   `{"name":...,"intrigue":...}` этот fallback не найдёт ничего. → В шаге 1 написать
   ОТДЕЛЬНЫЙ tolerant-парс объекта: снять ```json fence (как в `_parse_json_array`
   строки 297-303), затем `json.loads`; на fail → `{}`/fail-soft. НЕ переиспользовать
   `_parse_json_array` как есть.

2. **`test_topics_build.py:60-61` упадёт** [code #1, подтверждено в коде]. Жёсткий
   `assert set(t.keys()) == {'name','msgs','anchor_channel_id','anchor_external_id'}`
   сломается при добавлении ключа `intrigue`. И `assert t['name'] == "стаб-тема"`
   при моке, возвращающем строку, после перехода на JSON-парс даст fail-soft
   `name="тема"`. → Шаг 1 обязан обновить этот тест: добавить `'intrigue'` в
   ожидаемый set И переделать моки `complete` на возврат JSON-строки
   (`'{"name":"стаб-тема","intrigue":"крючок"}'`), а не голой строки.

## Важное

3. **Моки `complete` во ВСЕХ тестах topics возвращают строку** [code #3, risks #4].
   `test_topics_build.py` (строки ~70: `lambda: "x"`, и др.) + любые места, где
   мокается `topics_mod.complete`. После JSON-парса все молча уйдут в fail-soft.
   → Шаг 1: обновить ВСЕ моки complete в тестах topics на валидный JSON.

4. **Фикстуры `test_topics_render.py` без `intrigue`** [code #4, подтверждено].
   Рендер ОБЯЗАН брать `.get('intrigue')`, не `t['intrigue']`, иначе KeyError на
   старых фикстурах. → Уже заложено в step_1 (использовать `.get`), но добавить
   явный тест: тема без ключа intrigue → не падает, старый формат.

5. **Telegram 4096 overflow в /summary** [risks #1 LIKELY]. Содержание тем
   (+150-400 симв.) + подстановка имён может выбить дайджест за лимит на топиках с
   многими участниками. boltalka в шаге 3 может НЕ выявить (зависит от топика).
   → Шаг 2: усилить в промпте, что общий лимит ~2800/≤3500 ВКЛЮЧАЕТ содержание тем
   (не добавка сверху). Шаг 3: проверить длину явно, отметить риск в progress.md.
   НЕ решаем MAX_FRAGMENTS/MAX_TOKENS здесь (out-of-scope), только следим.

6. **`max_tokens ~120` для /topics — тильда + риск обрезки** [risks #2, structure #2].
   JSON с кириллической интригой (~140 симв = ~70-90 токенов) + name + синтаксис
   может не влезть в 120 → обрезанный JSON → fail-soft без сигнала. → Зафиксировать
   ОДНО число (не «~120»): поставить max_tokens=200 (запас на кириллицу), убрать тильду.

7. **Шаг 3 `--check || true` — нет критерия «ожидаемое vs аварийное расхождение»**
   [structure #1]. Исполнитель увидит РАСХОЖДЕНИЕ и не поймёт, норма это (правки
   /summary) или поломка /topics. → Уточнить в step_3: расхождение по /summary-кейсам
   ОЖИДАЕМО (меняли блок тем); /topics golden не покрывает вообще (проверяется
   глазами smoke-командой). Аварийно = пустой текст / голые `[@N]` / обрыв.

8. **Нет `pytest` ДО первой правки (baseline зелёности)** [structure #3, risks #5].
   План отмечает, что golden-baseline снять «до» не вышло. → Добавить в step_1 ПЕРВЫМ
   действием `python -m pytest -q` чтобы зафиксировать: всё зелёное ДО правок (иначе
   непонятно, чья поломка). Это и закрывает «golden baseline недоступен» частично.

## Мелочи

- Устаревшие докстринги в `topics.py` / `topics_render.py` (контракт dict темы без
  `intrigue`) — обновить по ходу [code].
- `[@N]` в блоке ГЛАВНЫЕ ТЕМЫ при t=0.4 LLM теоретически может нарушить запрет
  [risks #6 THEORETICAL] — grep проверяет промпт, не вывод; критик это ловит. Не блокер.
- step_4: уточнить «проектный CLAUDE.md» (не глобальный) [structure].
- `.baseline.txt` может отсутствовать — step_3 уже это допускает [structure].

## Противоречия между ревьюерами
Нет. structure говорит «критичного нет», code нашёл 2 блокера — это не противоречие:
структурно план целен, блокеры — на уровне кода (тесты + парс), их structure-ревью
не смотрит по роли.

## Покрытие
Coverage: все 15 требований закрыты, незакрытых нет, лишних шагов нет.

## Рекомендации
1. **step_1**: (а) написать ОТДЕЛЬНЫЙ парс JSON-объекта, не `_parse_json_array`
   (#1); (б) обновить `test_topics_build.py` — set ключей + JSON-моки (#2, #3);
   (в) тест рендера без ключа intrigue (#4); (г) max_tokens=200 фикс (#6);
   (д) первым действием `pytest -q` baseline (#8).
2. **step_2**: подчеркнуть, что лимит символов ВКЛЮЧАЕТ содержание тем (#5).
3. **step_3**: критерий ожидаемого vs аварийного расхождения golden (#7);
   явная проверка длины (#5).
4. Мелочи — по ходу.
