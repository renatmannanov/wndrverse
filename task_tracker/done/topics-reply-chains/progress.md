# Progress Log — topics-reply-chains

## Контекст для агента

**Что это:** reply-цепочки (вариант A) для hot-topics: build_chains склеивает
треды в документы ДО кластеризации. Лечит дефекты из диагностики 2026-06-10
(реакции кластеризуются отдельно от исходника, треды рвутся, мега-склейки).

**Что НЕ ломать:** контракт `build_topics` → `[{name, msgs, anchor_channel_id,
anchor_external_id}]`; `delivery/topics_render.py`; `delivery/cli.py`
(build_topics_digest/парсер); `/topics`- и `/summary`-боты; digest-пайплайн
(synthesis); `hotness.cluster_stats` (старая, остаётся для совместимости).

### Ключевые решения (зафиксированы с пользователем 2026-06-10)
- Вариант A (склейка), НЕ B (контекстные эмбеддинги).
- Серии: один автор, разрыв ≤ 300с → склейка (части лонгридов без reply).
- Эмбеддинг документа = среднее ИМЕЮЩИХСЯ векторов substantive-сообщений,
  взвешенное по длине текста. БЕЗ ре-эмбеддинга (нет нового OpenAI-спенда).
- msgs в дайджесте = только содержательные; likes/authors — по всем
  сообщениям треда (реакции дают вклад в hotness).
- Telegraph-страницы — backlog/telegraph-cluster-pages.md, не здесь.

### Факты о коде/данных (проверено 2026-06-10 на локальной БД)
- `metadata.reply_to_msg_id` есть у 2259/3668 сообщений boltalka (62%).
  Тип в metadata — строка; сравнивать строково с msg_id из external_id.
- Короткие (<80) сообщения ВСЕ эмбеджены (1925/1925) — выборка
  get_embedded_fragments_for_period (min_chars=1) уже отдаёт реакции с лайками.
- Контрольный тред: лонгрид Стаса = серия 14067 (root, 3934 зн.) + 14069 +
  14070, у всех reply_to=None (связь только серийная!); ответы реплаями
  14082/14083/14084/14093/14095/14100/14101/14104/14120/14122/14172/14194/14283
  → на 14067/14069/14070. Это acceptance-кейс шага 4.
- Анкор-фикс ab6af26 уже в master (probability ≥ 0.9 на уровне сообщений,
  константа ANCHOR_MIN_PROBABILITY) — шаг 3 переносит его на документы.
  ВНИМАНИЕ: ab6af26 ещё НЕ на проде — уедет вместе с деплоем этого плана.
- Диагностика с составами кластеров «до» — в переписке 2026-06-10; цифры:
  707 фрагментов → 325 после флуд-фильтра → 21 кластер + 80 шум;
  rank#1 = мега-склейка двух разговоров; реакции на лонгрид = отдельный
  кластер (19 сообщ.) от самого лонгрида (16 сообщ.).
- `cluster_embeddings` (clustering.py) — чистая, менять не нужно: ей всё
  равно, чьи векторы (сообщений или документов).
- Telegram-бот на VPS, прод-деплой только по команде пользователя.

### Тонкости
- ЦИКЛИЧЕСКИЙ ИМПОРТ (ревью 2026-06-10): `_is_substantive`/`_WORD_RE`
  переезжают в chains.py (шаг 2), topics.py только реэкспортирует. chains.py
  НЕ импортирует из topics.py.
- build_chains должен переживать отсутствие msg_id/reply_to_msg_id в dict'ах
  (`f.get(...)`) — старые тесты topics_build шлют синтетику без этих полей.
- created_at в выборке — ISO-строка (.isoformat()), для серийных разрывов
  парсить fromisoformat.
- PYTHONUTF8=1 на Windows для всех команд (кириллица/эмодзи).

## Learnings

### Шаг 1 — done (2026-06-10)
- boltalka/май: total 707, with reply_to 486, with msg_id 707, without msg_id 0
  (легаси-ключей в этом срезе нет). Sample: 13565 <- reply to 13548.
- Все 127 тестов зелёные после правки стора.

### Шаг 2 — done (2026-06-10)
- core/brain/chains.py: build_chains (union-find, reply + серии ≤300с),
  _is_substantive/_WORD_RE переехали сюда; topics.py реэкспортирует.
- ЛОВУШКА: при переносе импортов из topics.py случайно удалил
  `from core.llm.client import complete` — тесты monkeypatch'ат
  topics_mod.complete, упали 4 шт. Вернул. 139 passed (12 новых test_chains).
- Замечено: в test_monologue_dropped облако A (один автор, шаг 60с) после
  шага 3 склеится в ОДИН документ серийной связью — тест всё равно должен
  пройти (1 документ → шум/min_authors). Проверить на шаге 3.

### Шаг 3 — done (2026-06-10)
- chain_cluster_stats в hotness.py (msgs=substantive, likes/authors по всем);
  build_topics на документах; анкор = root самого раннего крепкого документа;
  LLM-сэмплы из flatten substantive. Контракт не менялся.
- ОТКЛОНЕНИЕ от плана («существующие тесты пройдут без правки данных»):
  test_monologue_dropped — облако A (один автор, шаг 60с) серийно склеилось
  в 1 документ, итого 7 точек → HDBSCAN весь в шум → 0 тем. Поправил
  ТАЙМСТАМПЫ облака A на часовые (>300с): снова 6 документов, тест опять
  проверяет именно min_authors (с комментарием в тесте). Сами тексты/вектора/
  сендеры не менялись.
- Урок для синтетики: реальному UMAP+HDBSCAN нужно ≥ ~10 точек-документов,
  6-7 уходят в шум целиком. Новый тест «реакции не склеивают два разговора»
  сделан на 24 фрагмента → 12 документов, реакции ≥80 зн. (как в реальном
  дефекте мега-склейки).
- 143 passed. CLI-смоук (платный) отложен в шаг 4 по плану.

### Шаг 4 — в работе (2026-06-10)

**ОТКЛОНЕНИЕ-ФИКС (серийная склейка):** глобальная сортировка «соседние пары»
из шага 2 ломала acceptance-кейс: 14068 (другой автор, та же секунда 13:22:52)
вклинился между 14067 и 14069 → серия рвалась. Переделано на пер-автора
смежность (соседние сообщения ОДНОГО автора, gap ≤ 300с) — это и было
намерение решения №1 плана. Тест test_series_survives_interleaved_other_author.
Синтетика двух старых тестов (two_clusters_ranked, monologue_dropped) раздвинута
с минутных на часовые таймстампы — иначе их одно-авторские сообщения с шагом
1-3 мин теперь честно склеиваются и точек для UMAP/HDBSCAN не хватает
(тексты/вектора/сендеры не менялись). 144 passed.

**Trace контрольного треда (выполнен):** документ с 14067 содержит 14069,
14070 и ВСЕ reply-ответы (14082…14283) — 80 сообщений, 40 substantive.
НО root = 14048, не 14067: разговор «хочу поменять работу» реально начался
с вопроса 14048, лонгрид Стаса — ответ в этом разговоре (реплаи его частей
сцеплены с репликами разговора). Семантически root=14048 корректен («начало
треда»), но письменный критерий ожидал 14067 → вопрос пользователю.

**Воронка (после фикса серий):**
- mcs=3: 707 фрагментов → 114 докуменов → 7 тем, шум 26 доков, fallback 0%
- mcs=2: 707 → 114 → 14 тем, шум 24, fallback 0%

**Вердикты по дефектам:**
- [дефект 1] УШЁЛ: отдельного кластера благодарностей нет ни при mcs=2, ни
  при mcs=3; реакции на лонгрид сидят в документе треда (trace).
- [дефект 2] При mcs=3 НЕ распался и стал шире: rank#1 = 38 доков склеивает
  safe-space (13567+) + кошки/кружочки (13608+) + смену работы (14048+).
  При mcs=2 РАСПАЛСЯ: #2 = смена работы (root 14048), #3 = safe space
  (13567), #1 = юмор-болталка (catch-all 19 доков). Reply-связей между
  разговорами нет — склейка только векторная (HDBSCAN eom).
- [дефект 3] Анкоры — корни тредов (root документов), fallback-rate 0%
  при обоих mcs (порог 0.9 работает и на документах, вопрос не требуется).

**Калибровка mcs:** по детерминированному правилу плана («3 если ≥5 тем») →
вышло бы 3 (7 тем). Но качество при 3 хуже: мега-кластер. Вопрос поднят
пользователю.

**Решения пользователя (2026-06-10):**
1. min_cluster_size = 2 (по качеству, отступление от детерминированного
   правила зафиксировано; комментарий-калибровка в build_topics обновлён).
   Числа: mcs=3 → 7 тем (rank#1 мега), mcs=2 → 14 тем (распался).
2. root = 14048 принят как корректный корень треда (разговор начался с
   вопроса 14048, лонгрид — ответ в нём). Критерий «root == 14067»
   скорректирован в PLAN.md.
3. Платный смоук подтверждён и прогнан.

**Платный смоук (mcs=2, --period 1m → срез с 2026-05-11, 277 фрагментов →
69 доков, 9 тем):** формат прежний (эмодзи+имя+(N)+ссылка), темы осмысленные:
«Поиск дома внутри себя» (26), «Творчество, путешествия и события» (20),
«Новый год и дни рождения» (22)… Кластера благодарностей нет, анкоры — корни
тредов. Digest-пайплайн проверен: `delivery digest --topic offerings --period
1w` работает (2 фрагмента → fallback-список, имена локально).

### Шаг 4 — done (2026-06-10)

### Шаг 5 — done (2026-06-10)
- Чеклист завершения выполнен целиком (см. step_5_completion.md).
- Решение по дальнейшему (НЕ делается сейчас): качество кластеров устроило →
  следующий кандидат из бэклога — `task_tracker/backlog/telegraph-cluster-pages.md`.
  Дефект 2 (мега-склейка) РАСПАЛСЯ при mcs=2 → вариант B (контекстные
  эмбеддинги) сейчас не нужен; вернуться к нему только если на других
  топиках/периодах снова появятся векторные мега-склейки.
- Прод-деплой НЕ выполнялся — по отдельной команде пользователя (вместе с
  фичей уедет анкор-фикс ab6af26).
---
