# Review Summary — digest-output-quality

> Дата: 2026-06-18
> Ревью: code + risks + structure + coverage

## Критичное (блокирует)

1. **PII-утечка в шаге 4 (критик)** [risks LIKELY] — шаг не фиксирует ЖЁСТКО,
   что `_critique` получает `content` (`[@N]`-текст ДО `humanize_author_refs`) +
   `grouped_text`. Если исполнитель передаст humanized-текст — реальные имена
   уйдут в OpenAI. Нарушение PII-контракта. → Прописать точные имена переменных
   и слот вставки (между стр.120 и 122 в `synthesize`, где `content` и
   `grouped_text` ещё `[@N]`).

2. **Нет `import json` в synthesis.py** [code] — `_critique` зовёт `json.loads()`,
   но модуль `json` в `core/brain/synthesis.py` не импортирован. Шаг 4 этого не
   упоминает. → Добавить импорт в шаг 4.

3. **Нет `tests/__init__.py`** [code] — `python -m tests.golden.run` (шаг 5) не
   запустится без пакета. → Шаг 5 создаёт `tests/__init__.py` И
   `tests/golden/__init__.py`.

4. **PII в git через снапшоты — порядок операций** [risks LIKELY] — шаг 5 не
   фиксирует, что `.gitignore` обновляется ДО первого прогона раннера. → Прописать
   порядок: (1) добавить `tests/golden/snapshots/` в `.gitignore`, (2) только
   потом первый прогон. Никаких коммитов между.

5. **Шаг 5: golden-раннер и `critic_issues`** [structure + code] — раннер зовёт
   `build_digest`, но `build_digest` НЕ прокидывает `critic_issues` из `synthesize`.
   Формулировка «если прокинет — опционально» = развилка для автономного агента. →
   Решить ОДНОЗНАЧНО: добавить проброс `critic_issues` в `build_digest` (мелкая
   правка в delivery/cli.py, вписать в шаг 4 или 5), либо убрать упоминание из раннера.

6. **Шаг 5: реальные даты кейсов** [structure] — «уточнить у пользователя» +
   примерные даты в файле = агент застрянет или создаст кейсы с 0 фрагментов. →
   Дать алгоритм: получить топики с данными через `get_topics_with_counts`, взять
   2–3 с наибольшим count, периоды — где реально есть сообщения. ОДИН путь.

## Важное

7. **Шаг 3: явный `min_chars=150` в вызовах** [risks CONFIRMED + code] — проверить
   `bot/ingest_bot.py`: `get_topics_with_counts(only=...)` зовётся БЕЗ явного
   min_chars (унаследует новый дефолт 80 — это ок). Но комментарий в
   `bot/ingest_bot.py:257` упоминает `min_chars=150` — после шага 3 станет ложным.
   → Шаг 3: обновить этот комментарий (сейчас сказано «не трогать»).

8. **Шаг 4: ветка `<3 fragments`** [risks LIKELY + structure] — «не запускать
   критик при <3» описано неконкретно. В коде эта ветка — ранний `return` в
   `synthesize` (стр.93-96), критик туда и так не дойдёт, если вставлен после
   Pass-2. → Уточнить: критик вставляется ПОСЛЕ `_synthesize_fragments`, ветка
   `<3` возвращается раньше — отдельной защиты не нужно, просто не вставлять выше.

9. **Шаг 2/4: проверить использование `result['model']` в проде** [risks LIKELY]
   — перед сменой проверить grep по `['model']` не только в tests, но и в
   delivery/digest/bot. → Добавить grep-команду в шаг 2.

10. **Шаг 5: публичность `parse_date_range`** [structure + risks] — функция есть
    в `delivery/cli.py` (модульного уровня, импортируется в bot). Импорт рабочий.
    → Указать в шаге 5 точный импорт: `from delivery.cli import parse_date_range, build_digest`.

11. **Шаг 6: какой CLAUDE.md** [structure + coverage] — «обновить CLAUDE.md» без
    пути. → Указать: ПРОЕКТНЫЙ `c:\Users\renat\projects\wndrverse\CLAUDE.md`,
    секция про digest-пайплайн.

## Мелочи

- `_looks_truncated`: в множестве терминальных — ASCII `"`, стоит добавить
  типографскую `»`/`"` (U+201D); кейса на закрывающую кавычку в тестах нет. [code]
- Кейс `offerings_all.json` с `null/null` = весь корпус → большой OpenAI-расход
  на каждом прогоне. Заменить на узкий период. [code]
- Шаг 4: `'model': COMPLETION_MODEL` в ветке `<3` (стр.96) менять НЕ надо —
  стоит явно сказать (касается только Pass-2). [code]

## Противоречия между ревьюерами

Нет. Code и risks независимо сошлись на PII-переменных критика (#1) и на
`critic_issues`/`build_digest` (#5) — это усиливает приоритет.

## Покрытие

Coverage: все 17 требований закрыты, лишних шагов нет. Дыр в покрытии НЕТ —
проблемы только в точности формулировок (развилки), не в полноте.

## Рекомендации

1. Поправить шаг 4: добавить `import json`, жёстко зафиксировать переменные
   `_critique(content, grouped_text)` с пометкой «оба `[@N]`, ДО humanize», явный
   слот вставки. (#1, #2, #8)
2. Поправить шаг 5: создать `tests/__init__.py`; порядок gitignore→прогон; узкие
   периоды кейсов; алгоритм выбора реальных дат через `get_topics_with_counts`;
   точные импорты. (#3, #4, #6, #10)
3. Решить однозначно проброс `critic_issues` через `build_digest` и вписать в
   шаг 4/5. (#5)
4. Шаг 3: дописать обновление комментария `bot/ingest_bot.py:257`. (#7)
5. Шаг 2: добавить grep по `['model']` в прод-коде. (#9)
6. Шаг 6: указать путь к проектному CLAUDE.md. (#11)
7. Мелочи — по ходу.
