# Backlog: Подключить агентов к общей БД сообщений сообщества

> Status: backlog
> Связано с: `task_tracker/todo/agent_template_claude_sdk/` (агент Клода)

## Контекст

У проекта уже есть БД сообщений сообщества (наполняется отдельно, лежит в другом месте — уточнить с Ренатом перед началом).

Сейчас агент Клода (см. план `agent_template_claude_sdk`) читает только Bus за 24ч + опционально Telethon. Когда дойдут руки — подключить чтение из общей БД как дополнительный источник.

## Что сделать

1. Уточнить где физически живёт общая БД сообщений и её схема
2. Создать в `agents/claude/` модуль `community_db_reader.py` — read-only клиент к этой БД
3. Расширить step "personal filter" в `main.py`: помимо Bus — подтягивать релевантные сообщения из общей БД для контекста
4. Аналогично подключить к будущим агентам Опенкло и Гермесу

## Не сейчас

- Без этого MVP агента Клода работает (читает Bus напрямую)
- Подключение БД — оптимизация контекста, не блокер
- Сначала запустить агента, потом улучшать источники
