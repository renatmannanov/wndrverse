# Environment
.env
*.env
.env.local

# Internal context (not for public)
internal/

# Python
__pycache__/
*.py[cod]
*.pyc
.venv/
venv/
env/

# Telegram sessions
*.session
*.session-journal

# Secrets
secrets/
*.key
*.pem

# Runtime data
data/
tmp_msgs.txt

# Topic map: contains real chat_ids (specific path, not *.json — would catch members.json)
core/ingest/topic_map.json

# DB dumps / backups — contain PII (message texts, names). Never commit.
*.sql
backup_*.sql

# Golden-set snapshots — humanized digests (real names/@handles). PII, never commit.
tests/golden/snapshots/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Claude Code local settings
.claude/
